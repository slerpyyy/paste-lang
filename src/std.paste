# paste - standard library

# Stack manipulation:
;{ 1 copy } dup =
;{ ;_ = } pop =
;{ ;__x = ;__y = __x __y } xch =
;{ 2 copy pop } over =

# Unary operators:
;{ 1 + } ++ =
;{ 1 xch - } -- =
;{ dup * } sq =
;{ ;0 ;1 ? } ! =

# Comparison operators:
;{ xch < } > =
;{ == ! } != =
;{ > ! } <= =
;{ < ! } >= =

# Basic math:
;{ xch 2 copy xch / floor * xch - } mod =
;{ (copy 2) < ;xch if pop } min =
;{ (copy 2) > ;xch if pop } max =
;{ dup 0 - max } abs =
